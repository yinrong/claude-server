{
  "name": "claude-server",
  "version": "2.0.0",
  "description": "Multi-agent orchestration hub for claude-code with memory and multi-client support",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "better-sqlite3": "^9.6.0",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
